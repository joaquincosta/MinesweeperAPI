spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: true
  datasource:
    url: jdbc:postgresql://localhost:5432/minesweeper
    username: postgres
    password: postgres
